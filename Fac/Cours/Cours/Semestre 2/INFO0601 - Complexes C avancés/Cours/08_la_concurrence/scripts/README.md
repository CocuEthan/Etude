# Scripts of course #8

## Compilation

To compile, type the following command:

```bash
make
```

## Programs/files

Here is the list of the generated programs or files used:
- `lock`: using a lock variable for mutual exclusion
- `alternation`: using a strict alternation for mutual exclusion
- `peterson`: Peterson algorithm